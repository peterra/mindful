@AGENTS.md

# Dev server

Frontend dev server runs on port **3333** (`npm run dev`).

Auto-started daily at 8:00 AM via launchd: `~/Library/LaunchAgents/com.mindful.dev.plist` (label `com.mindful.dev`, logs at `/tmp/mindful-dev.log`).
